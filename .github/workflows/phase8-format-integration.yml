name: phase8-format-integration

on:
  push:
    branches:
      - phase-08-rag-pipeline-controlled-ablations

permissions:
  contents: write

jobs:
  format-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase-08-rag-pipeline-controlled-ablations
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install pinned Ruff
        run: python -m pip install ruff==0.12.0
      - name: Format integration test and remove temporary workflow
        run: |
          ruff format backend/tests/integration/test_phase8_rag_integration.py
          rm .github/workflows/phase8-format-integration.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/tests/integration/test_phase8_rag_integration.py .github/workflows/phase8-format-integration.yml
          git commit -m "style: apply canonical Phase 08 integration formatting"
          git pull --rebase origin phase-08-rag-pipeline-controlled-ablations
          git push origin HEAD:phase-08-rag-pipeline-controlled-ablations
