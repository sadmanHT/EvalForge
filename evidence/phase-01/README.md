# Phase 01 Evidence Directory

Do not fabricate or hand-edit generated model-smoke evidence.

Phase 01 completion evidence now preserved here:

- `test-report.txt` — cumulative Phase 01 test output.
- `static-validation.txt` — `scripts/validate_phase1.py` output.
- `coverage-report.txt` — critical-contract coverage evidence.
- `clean-environment-report.txt` — fresh-copy rerun evidence.
- `model-smoke.json` — immutable Kaggle GPU smoke evidence for the exact frozen model revision.
- `model-smoke.txt` — human-readable smoke summary generated by the same notebook.
- `environment.txt` — captured `nvidia-smi` runtime evidence.
- `SHA256SUMS.txt` — checksums generated alongside the GPU smoke artifacts.
- `exit-gate-current.txt` — final machine-checkable Phase 01 hard-gate output.

The GPU evidence records `mistralai/Mistral-7B-Instruct-v0.3` at revision
`e8737b84b4470b28db3a0be719b362b1bd39a14d`, loaded on Tesla T4 hardware
with the 4-bit NF4 compatibility path, and a strict structured-output prediction
of canonical root-cause code `n_plus_one_query`. The 4-bit load mode is evidence
for Phase 01 compatibility only; it does not pre-decide the final benchmark
inference configuration used by later phases.
