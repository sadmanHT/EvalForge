# Phase 03 normalized public-incident candidate snapshots

These files preserve the exact EvalForge review facts used for candidate-level taxonomy mapping. They are normalized semantic snapshots with source URLs and observation metadata; they are deliberately not represented as byte-for-byte copies of the original public pages/issues. Therefore they do not, by themselves, satisfy the Phase 03 original-primary-source preservation requirement for research admission.
