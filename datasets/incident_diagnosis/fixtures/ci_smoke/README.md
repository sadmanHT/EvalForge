# Phase 03 CI smoke fixture

This directory is deterministic **non-research fixture data** used only for CI and invariant tests.
It must never be counted as benchmark evidence or merged into a research train/validation/test manifest.
