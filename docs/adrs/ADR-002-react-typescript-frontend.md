# ADR-002 — React + TypeScript Frontend

**Status:** Accepted

Use React + TypeScript for the evaluation product. Typed API models, explicit loading/error states, and browser end-to-end tests are required. The UI is a projection of stored experiment evidence; research numbers may never be hard-coded or manually substituted.
